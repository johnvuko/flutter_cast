name: cast
description: Dart package to discover and connect with Chromecast devices
version: 2.2.0
homepage: https://github.com/johnvuko/flutter_cast

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  bonsoir: ^6.0.1
  flutter:
    sdk: flutter
  protobuf: ^5.0.0

dev_dependencies:
  lints: ^6.0.0
  protoc_plugin: ^23.0.0
